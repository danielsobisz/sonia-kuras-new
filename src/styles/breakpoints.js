const size = {
  xs: "320px",
  m: "978px",
  lg: "1200px",
};

const device = {
  xs: `(max-width: ${size.xs})`,
  m: `(max-width: ${size.m})`,
  lg: `(max-width: ${size.lg})`,
};

export default { size, device };
